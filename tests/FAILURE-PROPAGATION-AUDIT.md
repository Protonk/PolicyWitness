1. Does step 5 permanently enforce every accepted C1–C6 consumer obligation, without weakening the questions or confusing readable labels with justified conclusions?

   Start with the original questions and accepted evidence/limits in `tests/FAILURE-PROPAGATION-CONTRACT.md`, then independently trace each obligation into registered tests. Compare the frozen acceptance expectations with the permanent JSON-only recovery helper, CLI/witness controls, Swift encoding/legacy tests and controller preservation/correlation tests. Check that expectations come from controlled inputs and observations rather than emitted classifications, that the consumer needs only one envelope, and that representative losses actually fail. Inspect the successful-spawn/failed-exec-result distinction, simultaneous limitations, legacy absence and independently newer controller evidence. Verify the ownership map, executed case coverage, internal failures/skips and source/build provenance against raw artifacts. Report concrete gaps or explain the evidence supporting completion; distinguish design justification, reporting enforcement and native observation.

2. Across steps 0–5, does PolicyWitness now fulfill the intended failure-propagation promise, or is there a remaining place where evidence is lost, overinterpreted or joined without adequate support?

   Reconstruct the overall promise in 150–200 words from the completed plan and the durable contract before following the handoff's conclusions. Then investigate the strongest plausible counterexample across the worker, host, validator, controller and optional log boundaries. Connect any finding materially to that reconstruction. Distinguish an implementation defect or unjustified claim from an explicitly accepted limit, and distinguish native evidence from fixture-supplied results and constructed interpretation. Check whether summaries preserve independent observations when several failures coexist. Give precise references and evidence that could falsify your conclusion; if you find no substantiated gap, state the scope and limits of your investigation rather than manufacturing one.

3. Can the completed plans be dissolved without leaving maintained repository files dependent on them, particularly `tests/FAILURE-PROPAGATION-PLAN.md`?

   Identify the plan documents retired by this closeout, then search files other than `*-PLAN.md` and `*-AUDIT.md` for remaining links or dependencies on those documents. At minimum, verify that code comments, existing `AGENTS.md` and `README.md` files, other named Markdown documents, fixtures, scripts and test messages are free of links to `tests/FAILURE-PROPAGATION-PLAN.md`. Search basename-only and relative links, absolute paths, fragments, prose references and instructions to consult the plan—not just the literal full path. Check that replacements lead to durable, sufficient contracts or coverage records, and that permanent tests do not read plan/audit files or temporary acceptance output. Report every remaining maintained reference with file/line and its consequence. State the search scope and exclusions explicitly; distinguish immutable historical evidence from live dependencies rather than silently hiding either. If clean, explain how you verified it.
